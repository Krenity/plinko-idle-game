{
  "name": "plinko-idle-game",
  "version": "0.1.0",
  "description": "A plinko-style idle/incremental game with physics",
  "type": "module",
  "scripts": {
    "dev": "bun run build && bun run server",
    "server": "bun run --watch server.ts",
    "build": "bun run typecheck && bun build src/main.ts --outdir dist --target browser",
    "start": "bun run dist/main.js",
    "lint": "biome lint src/",
    "format": "biome format --write src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi.js": "^7.3.0",
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/matter-js": "^0.19.0",
    "typescript": "^5.0.0",
    "@biomejs/biome": "latest"
  }
}
